{{- $fullname := printf "%s-%s" .Release.Name .Chart.Name | trunc 63 | trimSuffix "-" -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $fullname }}-secret
  labels:
    app: {{ $fullname }}
type: Opaque
data:
  apiToken: {{ .Values.secret.apiToken | b64enc | quote }}
